.. _rust_hello:

Minimal Rust Hello
##################

Overview
********

This is a small sample of writing an application in Rust on Zephyr. At this point, many things are
hardcoded, so this isn't particularly useful, but it doesn't work on Cortex-M4 targets configured
without floating point.
